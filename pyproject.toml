[project]
name = "pydantic-security-skills"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "anyio>=4.0",
    "duckdb>=1.5.5",
    "logfire[system-metrics]>=4.37.0",
    "polars>=1.0.0",
    "pydantic-ai>=2.9.1",
    "pydantic-ai-harness[codemode]>=0.7.0",
    "pydantic-evals>=2.20.0",
    "pydantic-monty>=0.0.18",
    "pyyaml>=6.0.3",
]

[project.scripts]
skill-runner = "skill_runner.runner:main"

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["skill_runner"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[project.optional-dependencies]
tui = [
    "textual>=8.2.8",
]
